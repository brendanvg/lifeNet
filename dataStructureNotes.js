//value is an array of objects, 
//each object is a node with 
//specific properties...
//group property searched to highlight
//and group like nodes 
//(node can be in more than one group in a network)
// 


key: network, 
value: [{
	nodeName : nodeName,
    group: group,
	position: x, y,
	edge: [[inEdge, inEdge],[outEdge,outEdge]
}]